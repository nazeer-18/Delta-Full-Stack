//write a program to check whether a given string is good or not a good string is one which starts with a and has a length greater than 3
let s= "app";
if(s[0]=='a' && s.length>3) {
    console.log("good string");
}else{
    console.log("Not a good string");
}